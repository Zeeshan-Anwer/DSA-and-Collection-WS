	Lc 5:- 

Q 1:-  Given an array, Each no appears twice except one no. which occurs only once.
Output that no which occurs only once.

Sol:- 	Hashmap increase space complexity 
 		a^a =0 
		Sort array or will work on unsorted array aswell.
		so add 0 to n to existing array, we get single occurance no the rest will be 0.
		Sort array (Onlogn)
		Unsorted array (On)

		psuedo code:- 
			
			a is array
			int ans=0	
			for(int i=0;i<n;i++)
			{
				ans=ans^a[i]
			}
			return ans;
			
	M.S Amazon 
Q 2:- Given an array which con3tains all numbers from 1->N except one no
	ie: size of array =N-1
	output the missing no.

Sol:- 	Given no sum will be less than original sum.
		Expected

		ans=0
		for(int i=1;i<=n ;i++)
	{
		ans=ans^i
	}
		for(int i=0;i<a.size;i++)
	{
		ans=ans^a[i]
	}
	return ans;

	Lc 6:-

Q :- All even found?

Sol: - 
	for(int i=0;i<n;i++){
		if(i & 1==0)
			print even
		}